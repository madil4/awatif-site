import { Navbar } from "./Navbar";

// export const Default = () => <Navbar />;
export const Default = () => (
  <p>fails with the A tag due to server-side-rendering</p>
);

export default { title: "Navbar" };
